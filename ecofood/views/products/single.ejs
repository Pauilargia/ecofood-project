
<div class="row">
  <div class="col-md-12 col-md-offset-1">
    <h1><%= product.name %></h1>
  </div>
</div>
<div class="row">
  <div class="col-md-12 col-md-offset-1">
    <div class="col-md-2">
      <img class="img-thumbnail" src="/<%= product.imageUrl %>" id="single-prod-img">
    </div>
    <div class="col-md-6">
      <h2>Price: <span><%= product.unitPrice %> € / <%= product.unit %></span></h2>
      <div class="col-md-12" id="prod-info">
      <p><%= product.description %></p>
      <p>Available quantity: <%= product.availableQty %> <%= product.unit %></p>
      <p>Available until: <%= product.deadline.toLocaleDateString("en-EN", { year: 'numeric', month: 'long', day: 'numeric' }) %></p>
      <p>Producer: <%= product.producer.username %></p>
      <p>Located in: <%= product.location %></p>
    </div>
  </div>
</div>

<div class="col-md-6 col-md-offset-1">
  <h4><a href="/">Go Back!</a></h4>
</div>
